# =============================================================================
# Portugal Road Watch - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
# =============================================================================

# ---------------------
# Supabase (required)
# ---------------------
# Each contributor runs their own local Supabase instance.
#
# 1. Install the Supabase CLI: https://supabase.com/docs/guides/cli/getting-started
# 2. Start the local instance (requires Docker):
#      supabase start
# 3. The CLI will output the API URL and anon key — paste them below.
#
# Example output from `supabase start`:
#   API URL: http://127.0.0.1:54321
#   anon key: eyJhbGciOiJI...
#
VITE_NEON_URL=http://127.0.0.1:54321
VITE_NEON_PUBLISHABLE_KEY=your-local-anon-key

# --- Optional variables ---
# Geocoding provider to use (e.g., 'nominatim', 'google', etc.)
# Leave blank to use the default provider
VITE_GEOCODING_PROVIDER=

# --- EMAIL PROVIDER ---
# Using Resend to send the emails so we need to configure the API key and the from email address.
RESEND_API_KEY=your-resend-api-key-here
RESEND_FROM_EMAIL=Buracos de Portugal <suporte@buracosdeportugal.pt>
